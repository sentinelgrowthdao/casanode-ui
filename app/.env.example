# Sentry DSN (leave empty to disable Sentry)
VITE_SENTRY_DSN=""

# Full API base URL (including /api/v1)
# Example: https://127.0.0.1:8443/api/v1 or /api/v1 if same origin
VITE_API_BASE_URL="/api/v1"

# Pre-shared authentication token (sent to /auth/login)
VITE_API_AUTH=""